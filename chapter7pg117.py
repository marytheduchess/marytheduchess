#input() function for prompting users for a series of names
#while loop to keep programs running as long as certain conditions remain true
#the input() fucntion pauses your program and waits for the user to enter some text
##once python receives the user's input, it stores it in a VARIABLE to make it convenient for you to work with

# ~ message = input("Tell me something, and I will repeat it back to you: ")
# ~ print(message)
#the input() function takes on argument: the PROMPT that we want to display so the user knows what to do.
#greeter.py pg. 118
# ~ name = input("Please enter your name: ")
# ~ print("Hello, " + name + "!")
#pg. 119 greeter.py
# ~ prompt = "If you tell us who you are, we can personalize the messages you see."
# ~ prompt += "\nWhat is your first name? "
# ~ name = input(prompt)
# ~ print("\nHello, " + name + "!")
# ~ ##Using int() to accept numerical input
# ~ ##when using the input() function, python interprets everything the user enters as a STRING
# ~ age = input("How old are you? ")
# ~ age = int(age)
#pg. 122 counting.py
current_number = 1 
while current_number <=5: 
	print(current_number)
	current_number += 1
	
	




